<div class="row">
  <div class="col-xs-12">
    <div class="box">
      <div class="box-header text-center">
        <h3 class="box-title title-catalog">Precios Genéricos</h3>
          <%= link_to 'Registrar', new_generic_price_path, class: "btn btn-info pull-right" %>
      </div>

      <div class="box-body table-responsive no-padding">
        <%= render 'shared/search', id: 'search-generic-prices',
          placeholder: 'Buscar por nombre',
          remote_url: '/generic_prices/search' %>
        <table class="table table-responsive table-hover table-condensed table-striped" id="generic_prices_table">
          <%= render 'body_generic_prices' %>
        </table>
      </div>

    </div>
  </div>
</div>

<%= render 'shared/paginate', elements_paginate: @generic_prices %>
