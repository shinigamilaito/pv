$("#sales_table").html("<%= j render('sales/body_sales') %>");
$('#total_cost_sales').html("<%= j render('sales/totals_costs', total_sales: @total_sales) %>");
$('#form-modal').modal('hide');
toastr['success']('Venta registrada correctamente.');

$(document).ready(function() {
    printPage(null, "<%= generate_ticket_sale_path(@sale.id, format: 'pdf') %>");
});
