$("#paid").html('<%= j render('paid') %>');
$(".link-hover").remove();
toastr['success']('Pago registrado correctamente.');
$("#header-services").html('<%= j render('services/header') %>');

$(document).ready(function() {
  $('#print-ticket').click();
});
