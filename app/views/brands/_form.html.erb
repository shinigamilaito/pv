<%= form_for(@brand, html: { class: "form-horizontal" }) do |f| %>
  <div class="box-body">
    <div class="form-group <%= 'has-error' unless @brand.errors[:name].blank? %>">
      <label class="col-sm-2 control-label">Nombre</label>
      <div class="col-sm-10">
        <%= f.text_field :name, class: "form-control", required: true %>
        <span><%= @brand.errors[:name].first %></span>
      </div>
    </div>
    <div class="form-group">
      <label class="col-sm-2 control-label">Especificaciones</label>

      <div class="col-sm-10">
        <%= f.text_area :specifications, class: "form-control textarea" %>
        <div class="invalid-feedback"><%= @brand.errors[:specifications].first %></div>
      </div>
    </div>
  <!-- /.box-body -->
  <div class="box-footer">
    <%= f.submit "Guardar", class: "btn btn-info pull-right btn-block btn-lg" %>
  </div>
  <!-- /.box-footer -->
<% end %>
