<%= collection_select :invitation, :subcategory_id, subcategories, :id, :name,
                      {prompt: 'Seleccione la subcategoría' },
                      class: 'form-control', required: true %>
