<div class="col-sm-12">
  <div class="card">
    <div class="card-header">
      <ul class="nav nav-tabs card-header-tabs">
        <li class="nav-item">
          <a class="nav-link <%= "#{'active' if @from_create_action.blank?} #{'show' if @from_create_action.blank?}" %>" data-toggle="tab" href="#home">Búsqueda de Equipos</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= "#{'active' if @from_create_action.present?} #{'show' if @from_create_action.present?}" %>" data-toggle="tab" href="#equipment_customer_div">Agregar Equipos</a>
        </li>
      </ul>
    </div>
    <div class="card-body">
      <div class="tab-content" id="myTabContent">
        <div class="tab-pane fade <%= "#{'active' if @from_create_action.blank?} #{'show' if @from_create_action.blank?}" %>" id="home">
          <%= render 'equipment_customers/search_form' %>
          <% if @equipment_customer.present? %>
            <%= render @equipment_customer %>
            <h5 class="card-title">Historico de Soportes</h5>

            <div class="accordion" id="accordionExample">
              <%= render 'supports/new' %>
              <%= render @equipment_customer.supports %>
            </div>
          <% end %>
        </div>

        <div class="tab-pane fade <%= "#{'active' if @from_create_action.present?} #{'show' if @from_create_action.present?}" %>" id="equipment_customer_div">
          <%= render 'equipment_customers/form' %>
        </div>
      </div>
    </div>
  </div>
</div>
